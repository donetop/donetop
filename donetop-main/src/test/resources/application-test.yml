spring:
  datasource:
    url: jdbc:h2:mem:testdb/
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      dialect: org.hibernate.dialect.H2Dialect
      format-sql: true
      show-sql: true